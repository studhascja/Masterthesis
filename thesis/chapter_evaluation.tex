\chapter{Evaluation} \label{sec:eval}
In this chapter, the test methodology, the different test scenarios and their results are presented.  
Chapter \ref{subsec:procedure} discusses the boundary conditions of the test runs.  
Subsequently, in Chapters \ref{subsec:low}, \ref{subsec:medium} and \ref{subsec:high}, the procedure is applied under environments with varying levels of interference.  
Finally, Chapter \ref{subsec:disc} compares the results of this thesis with findings from related work and places them in context.  

\section{Test procedure} \label{subsec:procedure}

Certain factors remain constant across all test runs.  
The test procedure is defined by five key elements:  
\begin{itemize}
    \item The \textbf{cycle time}, which in this thesis is fixed at 3 ms as previously described.  
    \item The \textbf{packet size and bandwith of bulk data transmission}, which must be determined to minimize interference with the real-time application and simultaneously deliver a reasonable amount of data.  
    \item The \textbf{duration of a single test run}, i.e., the time during which the RT-application executes its computation.  
    \item The \textbf{number of repetitions} of each test run, i.e., how often a given test case is executed to produce sufficient data for reliable conclusions.  
    \item The \textbf{different test cases}, derived from the Wi-Fi factors outlined in Chapter \ref{sec:implementation}.  
    In this thesis, a total of 112 different test cases are considered.  
\end{itemize}  

To determine the execution time of the real-time application, both the number of repetitions and potential clock drift must be taken into account.  
In this thesis, each test case is executed for 60 seconds.  
As described in \cite[p. 3]{drift}, quartz clocks can exhibit a drift of 1-100 ppm (parts per million).  
This corresponds to a drift of 60 $\mu$s to 6 ms per minute.  
However, in the results of this thesis, no noticeable drift was observed within a one-minute interval.  

With a cycle time of 3 ms, each 60-second run consists of 20,000 cycles.  
Each test case is repeated three times, resulting in 60,000 cycles per case to increase the probability of capturing anomalies.  
Additionally, before each run, the system clock is synchronized, effectively eliminating clock drift.  
The Wi-Fi connection is also given a warm-up period of 60 seconds, as recommended in \cite[Chap.~3.3.3.14]{alexander-bmwg-wlan-switch-meth-01}, to avoid distortions in the results.  

Preliminary experiments were conducted to select the throughput and packet size.  
The results are presented in Table~\ref{tab:throughput} and Table~\ref{tab:packetsize}.  

For the tests regarding the impact of the bulk data volume, the \ac{UDP} protocol with the standard packet size of 1440~Bytes was used.  
The selection of the bulk data volume is challenging, as each standard has a different maximum data rate depending on modulation and available bandwidth.  
Therefore, the bulk data volume would ideally need to be chosen proportionally to the maximum data rate in each case.  

However, this work aims to investigate the limits of real-time capability of Wi-Fi when it simultaneously transmits a certain amount of additional data.  
For this reason, a fixed amount of bulk data is selected, which remains identical across all standards and bandwidths.  
Consequently, standards with higher maximum data rates experience a lower relative load from the bulk data and therefore benefit in terms of real-time performance.  
This decision is motivated by the fact that, in practice, the relative load caused by other data traffic is not always known, and thus a Wi-Fi system with higher capacity inherently provides an advantage.  

\begin{table}
\begin{tabular}{||c|c|c||}
\hline\hline
\textbf{Throughput} & \textbf{Average Latency ($\mu$s)} & \textbf{Maximum Latency ($\mu$s)} \\
\hline\hline
5 Mbit & 910 & 75630 \\
\hline
20 Mbit & 980 & 238341 \\
\hline
30 Mbit & 1170 & 81920 \\
\hline
50 Mbit & 1320 & 254612 \\
\hline\hline
\end{tabular}
\caption{Comparison of the impact of different bulkloads on the Wi-Fi connection on the average and maximum execution time of one cycle of the real-time application.}
\label{tab:throughput}
\end{table}

Table~\ref{tab:throughput} presents the results of the preliminary experiments regarding the bulk data volume.  
The table shows the average and maximum latency\footnote{Latency is defined as the time required to complete a cycle of the real-time application.} of one cycle of the real-time application for data rates of 5, 20, 30 and 50~Mbit/s.  

It can be observed that, in particular, the maximum latency increases significantly with higher data rates.  
It is also evident that the maximum latency at 20~Mbit/s is higher than at 30~Mbit/s.  
This effect can be attributed to an unfavorable execution-time constellation in the test run at 20~Mbit/s.  
This explanation is supported by the average latency, which increases continuously with the data rate.  

The maximum data rate of Wi-Fi~4 with 20~MHz bandwidth is 76.9~Mbit/s.  
Thus, the results of the test case with 20~Mbit/s are still realistic, but for this study, a data rate of 30~Mbit/s is selected.  
The test runs with 5~Mbit/s show up to 45\% lower maximum latency in the low-bandwidth cases compared to the runs with 30~Mbit/s.  
However, in a network that carries both real-time and bulk data traffic, it is not realistic to limit the non-real-time traffic to 5~Mbit/s.  
At 30~Mbit/s, the maximum data rate of Wi-Fi~4 with 20~MHz bandwidth is utilized by 39\%, and the preliminary experiments did not show significant degradations compared to 20~Mbit/s.  

Preliminary experiments were also conducted for the selection of the packet size.  
The results are presented in Table~\ref{tab:packetsize}.  
These experiments were carried out using a data rate of 30~Mbit/s with the \ac{UDP} protocol.  
It can be observed that the packet size has a significant influence on the latency.  

\begin{table}
\begin{tabular}{||c|c|c||}
\hline\hline
\textbf{Package size (Bytes)} & \textbf{Average Latency ($\mu$s)} & \textbf{Maximum Latency ($\mu$s)} \\
\hline\hline
512    & 1000 & 69169 \\
\hline
1440   &  980 & 61506 \\
\hline
\rowcolor{green!30}
4096   &  910 & 37784 \\
\hline
8192   & 1120 & 38421 \\
\hline
16384  & 1280 & 41800 \\
\hline\hline
\end{tabular}
\caption{Comparison of the impact of different packet sizes on the average and maximum execution time of one cycle of the real-time application.}
\label{tab:packetsize}
\end{table}

The optimal packet size, as shown in green in Table \ref{tab:packetsize}, is 4096 bytes per packet.  
At this size, both latency and jitter are significantly lower than with 512-byte packets or the standard size of 1440 bytes.  
Furthermore, both latency values are lower than for larger packet sizes.  
However, it is noticeable that larger packet sizes perform better than smaller ones.  

Based on the results of the preliminary experiments, this study employs a data rate of 30~Mbit/s with a packet size of 4096~Bytes for the bulk data.  

The test cases are defined as combinations of Wi-Fi standard, frequency, bandwidth, \ac{QoS}, and transport protocol.  
For Wi-Fi~4, three combinations exist: the 2.4 GHz band and in the 5 GHz band both 20 MHz and 40 MHz channel widths.  
For Wi-Fi~5, the 5 GHz band with 20 MHz, 40 MHz and 80 MHz channel widths yields three combinations.  
Wi-Fi~6 supports all three frequency bands.  
In the 2.4 GHz and 5 GHz bands, four combinations arise, while in the 6 GHz band, the available channel widths of 20, 40, 80 and 160 MHz yield four more combinations.  
In total, Wi-Fi~6 therefore contributes eight combinations.  
Together, across all standards, 14 unique combinations of standards, frequency bands, and bandwidths are considered.  

Each of these 14 combinations is tested with four differnet levels of \ac{QoS} and with both \ac{TCP} and \ac{UDP}.  
This results in a total of $8 \times 14 = 112$ test cases.  

In the following, the test runs in an environment with low interference are discussed first.  

\section{Low disturbance test} \label{subsec:low}

In this test scenario, the experiments were conducted in an \ac{EMC} laboratory to bypass theoratical interference factors.  
The objective of these experiments is to evaluate the behavior of the different test cases in an ideal environment.  
The results can subsequently be used to assess the impact of real-world conditions on the Wi-Fi connection.  
In the \ac{EMC} laboratory, neither external electromagnetic interference nor additional influencing factors such as electrical devices or human presence are present.  

The results of the experiments are illustrated in Figures \ref{fig:tcp_low} and \ref{fig:udp_low}.  
A distinction is made between \ac{TCP}- and \ac{UDP}-based test cases.  
The naming convention of the test cases follows the pattern: Standard-Frequency-Bandwidth-QoS-Protocol.  
A value of “1” in the \ac{QoS} field indicates that \ac{QoS} is enabled.  
Specifically, the value 0xB8 was set as the \ac{DSCP} flag.  
This corresponds to the \ac{EF} class, which is intended for latency-critical applications, as described in the official \ac{IETF} document RFC 3246 \cite{rfc3246}.  
This class is particularly designed for voice and video applications that require low latency and minimal jitter.  

\begin{figure}[ht]
    \centering
    \resizebox{\textwidth}{!}{\input{figures/jitter_tcp_boxplot_3.pgf}}
    \caption{Presentation of the jitter of the TCP test cases in a low-interference environment.}
    \label{fig:tcp_low}
\end{figure}

Figure \ref{fig:tcp_low} presents the test cases using the \ac{TCP} protocol.  
It can be observed that test cases with enabled \ac{QoS} consistently exhibit significantly lower maximum values. 
An improvement of up to 70\% is achieved in some cases.
The mean values are also lower in most cases, although the difference is comparatively small.  
A similar behavior is observed in the \ac{UDP} test cases shown in Figure \ref{fig:udp_low}.  

Among the phases described in Section \ref{sec:implementation}, Phases 3 and 6 account for the largest share of the cycle time, together exceeding 90~\% in nearly all cases.  
These phases begin with the transfer of the packet to the driver queue via the Tracepoint \texttt{net\_dev\_xmit} and end with the confirmation of reception at the target device via the Tracepoint \texttt{netif\_receive\_skb}.  
A finer subdivision is not possible within this part of the network stack since no additional tracepoints exist.  
The significantly reduced jitter maxima in test cases with enabled \ac{QoS} affect only Phases 3 and 6.  
This is due to the preferential treatment of packets with the \ac{DSCP} flag set in both the driver and the firmware.  
As described in \cite{rfc3246}, the standard for the \ac{EF} class specifies shorter queues and reduced backoff times.  
Consequently, the delays within the driver and firmware are reduced, preventing occasional high latencies.  
However, this is not guaranteed, as illustrated by test case 6-5-80-1-TCP in Figure \ref{fig:tcp_low}, which shows comparatively high maximum values despite \ac{QoS} being enabled.  

\begin{figure}[ht]
    \centering
    \resizebox{\textwidth}{!}{\input{figures/jitter_udp_boxplot_3.pgf}}
    \caption{Presentation of the jitter of the UDP test cases in a low-interference environment.}
    \label{fig:udp_low}
\end{figure}

It is also noteworthy that the \ac{TCP} test cases consistently show lower mean jitter and mean latency compared to the \ac{UDP} test cases.  
This observation is unexpected, as the connection establishment and error correction mechanisms in \ac{TCP} would typically suggest lower performance.  
The maximum values, however, are approximately the same for both protocols.  

Another unexpected observation concerns the test cases with 80~MHz bandwidth, which in most instances show comparatively poor results.  
Both with \ac{TCP} and \ac{UDP}, the test cases using 80~MHz bandwidth exhibit the highest jitter maxima.  
Out of eight test cases with 80~MHz bandwidth, six show a relatively high maximum jitter.  
Only test case 5-5-80-1 exhibits low maximum values, both for \ac{TCP} and for \ac{UDP}.  

It was initially expected that test cases with lower bandwidth would show poorer performance, since the channel is more heavily utilized.  
This expectation is generally confirmed, as both the average and maximum jitter tend to decrease with increasing bandwidth.  
However, the 80~MHz test cases form an exception to this trend.  

A correlation between the factors standard and frequency in the test cases and the jitter could not be identified.  

Having established the behavior of the test system under ideal conditions, the following section examines the results in a more realistic environment.  

\section{Average disturbance test}  \label{subsec:medium}

\ref{fig:mid_tcp} and \ref{fig:mid_udp} illustrate the results of the test runs in an environment with average interference.
An environment with medium interference corresponds to a typical office or residential setting.  
In such an environment, other networks and electrical devices exist that may cause interference.  
However, there are no devices or networks deliberately deployed to disturb the test system.  

Figures \ref{fig:mid_tcp} and \ref{fig:mid_udp} show the results of the test runs for the maximum and average jitter.  
The jitter values are expressed as a percentage relative to the jitter observed in the low-interference environment.  
This test was already described in Section \ref{subsec:low}.  

In general, the results show that the maxima increased more strongly for the \ac{TCP} test cases than for the \ac{UDP} test cases.  
By contrast, the mean jitter increased more strongly for \ac{UDP}.  
Both protocols, however, exhibit an overall increase in jitter.  
Nevertheless, the average jitter for \ac{TCP} remained unchanged.  

It is also evident that test cases with \ac{QoS} generally show a smaller increase in jitter.  
For both protocols, all high outliers in both average and maximum jitter occur in test cases without \ac{QoS}.  
In particular, in the 6 GHz band, a notably small increase in jitter can be observed.  
This can primarily be attributed to the fact that, at the time of this study, only few devices operated in this frequency band, resulting in minimal interference.  
However, Wi-Fi Standard 6 shows higher increases in jitter, both in maximum and average values, compared to Standards 4 and 5.  
Among these, Standard 4 performs best.  
This can be seen clearly in Figure \ref{fig:mid_tcp}, particularly for test cases 6-5-20-0-TCP and 6-5-80-0-TCP.  
In Figure \ref{fig:mid_udp}, the test case 6-2.4-20-0-UDP stands out.  

In the \ac{UDP} test run, the test case 6-2.4-20-0-UDP is particularly affected by interference, showing a maximum jitter level eight times higher than the average maximum increase.  
In the \ac{TCP} test run, the test case 5-5-40-0-TCP is most noticeable, exhibiting a maximum jitter level seven times higher than the average maximum increase.  
Such outliers occur in almost all test configurations, with the sole exception of the test case 6-6-160-*-*.  
Outliers are especially present in the 2.4 GHz band with 20 MHz bandwidth and in test runs with 80 MHz bandwidth, meaning the presented test runs represent the general trend well.  
Furthermore, it is noticeable that outliers occur significantly less frequently in test runs with \ac{QoS} and exhibit lower magnitudes.  

Outliers always occur in phases 3 and 6, i.e., in the link and hardware layers of the network stack.  
In these cases, the client side is more strongly affected in terms of both average and maximum jitter.  
As shown in Figure \ref{fig:pi_wifi_general}, both devices in the RT connection must send and receive data.  
In contrast, during bulk data transmission, only the client sends data while the server only receives.  
From this, it can be concluded that data transmission in a Wi-Fi connection is more susceptible to interference than data reception.  

This work considers five levels of \ac{QoS}.  
\begin{itemize}  
    \item \textbf{\ac{QoS} 0} means that no \ac{QoS} is applied.  
    \item \textbf{\ac{QoS} 1} uses the \ac{DSCP} value 0xB8, which corresponds to the \ac{EF} class and is, as described in RFC 3246 \cite{rfc3246}, specifically intended for latency-critical applications.  
    \item \textbf{\ac{QoS} 2} uses the CS7 class, which \cite[p. 27]{rfc4594} recommends for control applications.  
          This class is not intended for user applications, but for network management and control tasks.  
          Since these run at a higher priority than user traffic, this work investigates whether this class provides advantages for real-time Wi-Fi connections.  
          As the test system in this study is a peer-to-peer setup, network management traffic cannot be disturbed, making the use of CS7 safe.  
    \item In \textbf{\ac{QoS} 3}, both the \ac{DSCP} value 0xB8 and the \ac{qdisc} \ac{HTB} are applied, as shown in Figure \ref{fig:qdisc}.  
          A root node (1:1) is configured with a data rate and a burst size, similar to the Token Bucket Filter described in Section \ref{subsec:qos}.  
          This root node is divided into two child nodes: 1:10 (RT data) and 1:30 (bulk data).  
          As described in \cite{htb}, each class can be assigned a priority.  
          In this work, as can be seen in Figure \ref{fig:qdisc} the RT class is assigned priority 0, and the bulk class priority 5.  
          Packets are queued according to their class into the queues shown in Figure \ref{fig:qdisc}.  
          The round-robin scheduler then always prioritizes packets from queue 0 as long as its rate and burst are not exceeded.  
          Therefore, the rate and burst of the RT node must be configured to ensure RT packets are always transmitted.  
          The root node is configured with 500 Mbit/s, of which 470 Mbit/s are allocated to the RT class and 30 Mbit/s to the bulk class.  
          This ensures reliable transmission of RT packets.  
          After being dequeued from the \ac{Qdisc}, packets are handed to the driver queue, and then via \ac{DMA} to the \ac{NIC}.  
          Using \ac{EDCA}, RT packets are placed into \ac{AC} 3 based on the \ac{DSCP} header, while bulk packets are placed into \ac{AC} 0.  
          This results in RT packets having an \ac{AIFS} of 2 slot times compared to 7 slot times for bulk packets.  
          Furthermore, the random backoff time ranges from 7–15 $\mu$s for RT packets, while it ranges from 31–1023 $\mu$s for bulk packets.  
    \item In \textbf{\ac{QoS} 4}, the \ac{EF} class is again used, but additionally the \ac{Qdisc} Prio is applied, also shown in Figure \ref{fig:qdisc}.  
          Unlike \ac{HTB}, Prio does not use rates and bursts, but classifies packets into three queues by default.  
          Queue 0 is always prioritized as long as it contains packets.  
          In this work, queue 0 is used for RT packets and queue 2 for bulk packets.  
          In terms of real-time performance, Prio functions similarly to \ac{HTB}, provided \ac{HTB} is configured with sufficiently high rates and bursts for RT traffic.  
          As described in \cite{prio_qdisc}, Prio is often used to minimize packet latency and is therefore considered in this work as well.  
          After leaving the \ac{Qdisc}, packets are handed to the driver queue and subsequently to the \ac{NIC} via \ac{DMA}, using the same \ac{EDCA} procedure as in \ac{QoS} 3.  
\end{itemize}  

All \ac{QoS} levels are implemented using \texttt{tc} and \texttt{iptables}, and all \ac{DSCP} values were verified per test case with Wireshark.  
The full configuration can be found in Appendix \ref{lst:qos}.  
To distinguish network traffic, packets were marked with \texttt{--set-mark 1} and separated by ports.  
Bulk data transmission was always performed on port 5202, while RT data transmission was carried out on port 8080.  
The \ac{DSCP} values were set using \texttt{--set-tos}.  
Priorities were assigned via \texttt{tc filter} for Prio and \texttt{tc class} for \ac{HTB}.  

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/qdisc.drawio.pdf}
    \caption{Presentation of the Prio and the HTB qdisc used in this work.}
    \label{fig:qdisc}
\end{figure}

The results of the tests with different \ac{QoS} levels are presented in Figures \ref{fig:qos_tcp_avg}, \ref{fig:qos_udp_avg}, \ref{fig:qos_tcp_max} and \ref{fig:qos_udp_max}.  
Here, a distinction is again made between \ac{UDP} and \ac{TCP}, as well as between maximum and average jitter.  

For the average jitter, it can be observed that for both \ac{UDP} and \ac{TCP}, all values are below the average jitter without \ac{QoS}.  
Furthermore, as shown in Figures \ref{fig:qos_udp_avg} and \ref{fig:qos_tcp_avg}, test cases with 20 MHz bandwidth and a frequency of 2.4 GHz perform the worst.  
It is also noticeable that across all \ac{QoS} levels, the jitter behaves similarly, with \ac{QoS} 1 performing slightly worse and \ac{QoS} 3 performing best.  
In general, a combination of \ac{DSCP} value and \ac{Qdisc} yields better results than using only a \ac{DSCP} value.  
The difference between the average jitter of \ac{QoS} 3 and the other \ac{QoS} levels is most pronounced in the \ac{UDP} cases.  
Overall, however, the jitter values are higher in the \ac{UDP} cases than in the \ac{TCP} cases.  

Regarding the maximum jitter, shown in Figures \ref{fig:qos_udp_max} and \ref{fig:qos_tcp_max}, different characteristics can be observed.  
As already noted in previous test runs, the \ac{UDP} and \ac{TCP} test cases are very similar in terms of maximum jitter.  
\ac{TCP} even performs slightly worse regarding maximum jitter, particularly in test case 4-5-40-TCP, where the \ac{QoS} 3 configuration exhibits a very large maximum jitter, as seen in Figure \ref{fig:qos_tcp_max}.  
In general, no clear patterns can be identified in the evaluation of the test cases with respect to maximum jitter.  
It is only noticeable that both \ac{TCP} and \ac{UDP} show distinct outliers in the measurement data, especially for the \ac{QoS} 3 configuration and to a lesser extent for \ac{QoS} 1.  
Interestingly, in both \ac{TCP} and \ac{UDP}, the test case 4-5-20 stands out as particularly affected.  
It is also evident that in the maximum jitter, all curves converge at test case 6-6-160-*, where the jitter is generally very low.  
This indicates that during this test case, no outliers occurred in any of the \ac{QoS} configurations.  

From the \ac{QoS} tests, it can therefore be concluded that for average jitter, \ac{QoS} 3 provides the best performance.  
However, for maximum jitter, no definitive conclusion can be drawn regarding the optimal \ac{QoS} configuration, except that a combination of \ac{Qdisc} and \ac{DSCP} value produces better results than using only a \ac{DSCP} value.  
It is also noticeable that tests using Wi-Fi 6 in the 6 GHz band with a bandwidth of 160 MHz perform particularly well.  

After discussing in this chapter how an environment with medium interference affects the real-time capability of Wi-Fi and how \ac{QoS} influences it, the following section examines how the real-time performance of Wi-Fi behaves in an environment with high interference.  

\section{High disturbance test} \label{subsec:high}
In the high-interference test, the same test cases are conducted as before, but an additional pair of Raspberry Pis is introduced.  
This second pair operates on the same frequency and with the same bandwidth, transmitting as many packets as possible via \texttt{iperf3}.  
The second pair of Raspberry Pis, referred to in the following as the \emph{noisy neighbour}, is equipped with an Atheros AR5BXB92 Wi-Fi card, as described in Section \ref{sec:used_hardware}.  
However, since this card only supports Wi-Fi 4, only test cases in the 2.4 GHz and 5 GHz bands are considered.  
For the test cases with 80 MHz bandwidth, the noisy neighbour uses only 40 MHz channels, as Wi-Fi 4 does not support wider channels.  
This setup allows the simulation of a worst-case scenario for the 2.4 GHz and 5 GHz bands, in which another Wi-Fi network in close proximity fully occupies the same channel on the same frequency band.  

The results are shown in Figures \ref{fig:high_udp} and \ref{fig:high_tcp}, again distinguishing between \ac{UDP} and \ac{TCP} test cases.  
For the \ac{QoS} test cases, \ac{QoS} level 3 was applied, since, as discussed in Section \ref{subsec:medium}, it produced the best results for average jitter and exhibited outliers in the maximum jitter only in isolated cases.  
In both figures, the maximum jitter is shown in blue and the average jitter in green.  
The average of the maximum jitter values is shown in red and the average of the mean jitter values in yellow.  
It can be observed that the \ac{TCP} and \ac{UDP} values are much closer together than in the low-interference scenario described in Section \ref{subsec:low}.  
It is also evident that the test cases with \ac{QoS} perform significantly better than those without \ac{QoS}.  
Compared to the laboratory experiment, the jitter increased by a factor of 10-20 in both average and maximum values.  

Furthermore, it is noticeable that the test case 6-6-160-1-* remained almost unchanged.  
This is because this test run was recorded as a reference and was not affected by interference in its frequency band.  
Accordingly, it can be seen that the test cases are not disturbed across frequency bands, which was expected.  
Surprisingly, however, the test cases with 80 MHz bandwidth did not perform better but often worse than the 40 MHz test cases.  
Since the noisy neighbour was operated only on a 40 MHz channel, only half of the frequency spectrum of the 80 MHz channels was interfered with, yet no improvement was observed.  

This chapter has examined how Wi-Fi behaves under heavy load in the worst possible interference scenario, the following section discusses the results of all three test series.  

\section{Discussion} \label{subsec:disc}
Nachdem in den zuvor beschriebenen Kapiteln die Messwerte und die Auswertung der Testdurchgänge in drei verschiedenen Umgebungen (Labor, Büro und mit Nachbarverbindung) dargestellt wurden, werden im Folgenden die Ergebnisse zusammengefasst und diskutiert.
Um die Ergebnisse, bzw die Auswirkungen der einzelnen Faktoren auf den maximalen Jitter darzustellen wird für jeden Testdurchgang ein Spinnen-Radar-Diagramm sowohl für den maximalen, als auch für den durchschnittlichen Jitter gezeigt.
Somit wird in Abbidung \ref{fig:r_mid} die Auswirkungen im Testlauf mit durchschnittlicher Belastung dargestellt.
Dabei wird in Abbildung \ref{fig:r_mid_max} die Auswirkungen auf den maximalen Jitter und in Abbildung \ref{fig:r_mid_med} die Auswirkungen auf den durchschnittlichen Jitter dargestellt.

\begin{figure}[h]
  \centering
  \begin{subfigure}{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/radar_mid_max.pdf}
    \caption{Presentation of the weighting of influencing factors on the maximum jitter in the test run with average disturbance.}
    \label{fig:r_mid_max}
  \end{subfigure}
\hfill
  \begin{subfigure}{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/radar_mid_med.pdf}
    \caption{Presentation of the weighting of influencing factors on the average jitter in the test run with average disturbance.}
    \label{fig:r_mid_med}
  \end{subfigure}
  \caption{Presentation of the weighting of influencing factors on the maximum and average jitter in the test run with average disturbance.}
  \label{fig:r_mid}
\end{figure}

Die Auswirkungen werden berechnet indem Testfälle it nur einem unterschiedlichen Parameter verglichen werden.
Wenn zum Beispiel der Einfluss des Faktor \ac{QoS} berechnet werden soll, werden die Testdurchgänge std:a-freq:b-bandwitdth:c-qos:0-prot:d ($config_1$) mit std:a-freq:b-bandwitdth:c-qos:1-prot:d ($config_2$) verglichen.
Dabei existiert für jeden Faktor eine Variable auf die der Einfluss pro Testfall addiert wird.
Der Einfluss auf den maximalen Jitter wird mittels $\frac{|max(config_1) - max(config_2)|}{\frac{max(config_1) + max(config_2)}{2}} \times 100$ berechnet. 
Somit wird der mittlere prozentuale Unterschied zwischen den beiden Testdurchgängen berechnet und anschließend auf die Varaible addiert.
Im Falle das \ac{QoS} 1 besser abschneidet, als \ac{QoS} 0 wird das Ergebnis auf die Variable von \ac{QoS} 1 mit minus addiert, als subtrahiert.
Dies wird für alle vergleichbaren Kombinationen durchgeführt.
Teilweise sind nicht alle Kombinationen vergleichbar, wie beispeisleweise beim Faktor Standard können nur begrenzte Standard 6 Testfälle verglichen werden, da alle anderen Faktoren gelcih sein müssen.
Da nur Standard 6 die Frequenz 6 GHz bietet sind all diese Testfälle nicht vergleichbar.

In der Abbildung \ref{fig:r_mid} besteht das Radardiagramm aus fünf Parametern, welche den fünf Faktoren entsprechen.
Somit wird Frequenz in blau, der Standard in gelbe, die Bandbreite in orange, \ac{QoS} in grün und das Protokoll in Türkis dargestellt.
Zudem werden innerhalb jeder der Kategorien die verschiedenen Parameter dargestellt.
Also für den Standard 2.4 GHz, 5 GHz und 6 GHz, für die anderen Faktoren ist dies äquivalent mit deren Parametern.
Jeder der Parameter hat einen Wert, dieser entspricht dem prozentualen Verhältnis zu den anderen Parametern.
In Abbildung \ref{fig:r_mid_max} wäre dies bei der 6 GHz Frequenz -81.7.
Das heißt dass im Durchschnitt die Testkonfigurationen mit 6 GHz 81.7 \% geringere Jitter aufwiesen, als die restlichen Frequenzen.
Somit ist ein negativer Wert (in grün dargesetllt) besser, als ein positiver (in rot dargestellt).
Das Netzdiagramm, welches über das Radardiagramm gelegt wurde, bemisst sich an der Addition der Beträge der Werte der einzelnen Parameter.

Es fällt in allen drei Testdurchgängen auf, welche in den Abbildungen \ref{fig:r_labor}, \ref{fig:r_mid} und \ref{fig:r_high} dargesetllt sind, dass für den maximalen Jitter der stärkste Faktor \ac{QoS} und für den mittleren Jitter das Protokoll ist, wobei \ac{TCP} dabei besser abschneidet.
Es ist überraschend, dass \ac{TCP} einen geringeren durchschnittlichen Jitter aufweist, als \ac{UDP}, da der Overhead von \ac{TCP} aufgrund der Conjestion Control und dme Acknoledgement Mechanismus einen größeren Overhead als \ac{UDP} besitzt.
Jedoch ist dieses Verhalten auch in anderen wissenschaftlichen Arbeiten aufgefalle.
Somit beschreibt Simon et al. in \cite[p. 227, 228]{tcp_vs_udp}, dass in seinen Ergebnissen \ac{UDP} eine doppelt so große Latenz besitzt, wie \ac{UDP}.
Auch Gaoyang et al. beschreiben in \cite[p. 11]{tcp_vs_ud}, dass vor allem bei größeren Datenpaketen \ac{TCP} niedrigere Latenzen aufweisen.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/latency_cdf_all.pdf}
    \caption[Visualization of cumulative probability at cycle time for different test runs.]{Representation of the percentage number of cycles that meet real time at a certain rate for chosen test runs.}
    \label{fig:percent}
\end{figure}


\begin{table}
\begin{tabular}{||p{3.5cm}|p{2.5cm}|p{2.5cm}|p{2.5cm}|p{2.5cm}||}
\hline\hline
\textbf{Testcase} & \textbf{\ac{WCET} \newline Application} & \textbf{\ac{WCET} \ac{Qdisc}} & \textbf{\ac{WCET} Driver and MAC} & \textbf{\ac{WCET} whole} \\
\hline\hline
$C_{24}$-6-6-160-1-tcp & 2.19 ms & 0.14 ms & 703.56 ms & 705.89 ms \\
\hline
$C_{mid}$-6-6-160-1-tcp & 0.22 ms & 0.05 ms & 2.27 ms & 2.54 ms \\
\hline
$C_{high}$-6-5-40-1-tcp & 2.15 ms & 0.05 ms & 39.26 ms & 41.46 ms \\
\hline
$C_{labor}$-6-6-160-1-tcp & 0.15 ms & 0.02 ms & 1.56 ms & 1.73 ms \\
\hline
$C_{ath9k}$-4-5-40-1-tcp & 0.14 ms & 0.03 ms & 6.16 ms & 6.33 ms  \\
\hline
$C_{mt7925e}$-4-5-40-1-tcp & 0.34 ms & 0.04 ms & 6.31 & 6.69 ms  \\
\hline\hline
\end{tabular}
\caption{Comparison of the WCET of different test runs}
\label{tab:wcet}
\end{table}
