\chapter{First Chapter of the Appendix}
\begin{table}
\caption[Overview of the used packages in the Yocto build system for the test environment.]{Overview of the used packages in the Yocto build system for the test environment. Highlighted in green are the packages developed as part of this work. Highlighted in orange are packages that are modified.}
\label{tab:used_packages}
\centering
\begin{tabular}{|p{3.5cm}|p{4cm}|p{8cm}|}
\hline
\textbf{Category} & \textbf{Package} & \textbf{Description} \\
\hline

% ==== Firmware ====
\multirow{3}{=}{\makecell[l]{\textbf{Firmware \&}\\\textbf{Hardware Support}}}
& kernel-modules & Kernel modules for drivers loaded at runtime. \\ \cline{2-3}
& linux-firmware & Firmware binaries for various hardware devices. \\ \cline{2-3}
& \cellcolor{green!30}pcie-overlays & PCIe device tree overlays. \\ \hline

% ==== Build & Dev ====
\multirow{7}{=}{\makecell[l]{\textbf{Build \&} \\\textbf{Development}\\\textbf{Environment}}}
& gcc & GNU C compiler. \\ \cline{2-3}
& make & Build automation tool. \\ \cline{2-3}
& clang & LLVM-based C/C++ compiler (rust-ebpf application). \\ \cline{2-3}
& cargo & Rust package manager and build system (rust-ebpf application). \\ \cline{2-3}
& rust & Rust compiler and toolchain. \\ \cline{2-3}
& python3 & Python 3 interpreter (test suite). \\ \hline
& libbpf & C library for working with eBPF programs (rust-ebpf application). \\ \cline{2-3}

% ==== Networking ====
\multirow{9}{=}{\makecell[l]{\textbf{Networking Tools}}}
& iproute2 & Network configuration (interfaces, routing, tunnels). \\ \cline{2-3}
& iproute2-tc & \ac{QoS} utilities. \\ \cline{2-3}
& net-tools & Network utilities (like ifconfig, netstat, also neede for the \ac{QoS} script). \\ \cline{2-3}
& iptables & Firewall and NAT management tool (\ac{QoS} script). \\ \cline{2-3}
& \cellcolor{orange!30}hostapd & Wi-Fi access point and authentication daemon (Server AP). \\ \cline{2-3}
& wpa\_supplicant & Wi-Fi client manager (Client). \\ \cline{2-3}
& iw & Command-line tool for Wi-Fi configuration. \\ \cline{2-3}
& iperf3 & Network performance and bandwidth test tool. (bulk data) \\ \cline{2-3}
& dhcpcd & DHCP client for automatic IP configuration. \\ \hline

% ==== Monitoring ====
\multirow{4}{=}{\makecell[l]{\textbf{Monitoring}}}
& trace-cmd & Kernel tracing and analysis tool (ftrace frontend). \\ \cline{2-3}
& perf & Performance profiler for CPU and system analysis. \\ \cline{2-3}
& tcpdump & Captures and analyzes network packets. \\ \cline{2-3}
& procps & Tools for process and system monitoring (ps, top, free).  \\ \hline

% ==== Rust Apps ====

\multirow{2}{=}{\makecell[l]{\textbf{Applications}}}
& \cellcolor{green!30}rust-application  & Rust Server-Client Testsystem, for \ac{UDP} and \ac{TCP}. \\ \cline{2-3}

& \cellcolor{green!30}test-system & Implements the Server and Client Test Suite and necessary helper scripts, as for the \ac{QoS} implementation \\ \hline

\end{tabular}
\end{table}

\label{a:somelabel}
\begin{listing}[H]
    \centering
\begin{minted}[fontsize=\small, escapeinside=||, linenos]{rust}
include!("bpf/monitor.skel.rs");
...
|\textbf{\textcolor{funblue}{set\_rt\_priority}}|(99);
...
let open_skel = |\textbf{\textcolor{kwgreen}{MonitorSkelBuilder}}|::|\textbf{\textcolor{funblue}{default}}|().|\textbf{\textcolor{funblue}{open}}|();
let mut skel = open_skel?.|\textbf{\textcolor{funblue}{load}}|()?;
skel.|\textbf{\textcolor{funblue}{attach}}|()?;

let maps = skel.|\textbf{\textcolor{funblue}{maps}}|();

let mut ringbuf_builder = |\textbf{\textcolor{kwgreen}{RingBufferBuilder}}|::|\textbf{\textcolor{funblue}{new}}|();
ringbuf_builder.|\textbf{\textcolor{funblue}{add}}|(maps.|\textbf{\textcolor{funblue}{events}}|(), move data: &[u8] {
    // Callback function
});

let _handle = |\textbf{\textcolor{kwgreen}{thread}}|::|\textbf{\textcolor{funblue}{spawn}}|((move ||{
    while true {
        ringbuf.|\textbf{\textcolor{funblue}{poll}}|(|\textbf{\textcolor{kwgreen}{Duration}}|::|\textbf{\textcolor{funblue}{from\_millis}}|(100).|\textbf{\textcolor{funblue}{unwrap}}|();
    }
});

let listener = |\textbf{\textcolor{kwgreen}{TcpListener}}|::|\textbf{\textcolor{funblue}{bind}}|("192.168.1.1:8080")?;

for stream in listener.|\textbf{\textcolor{funblue}{incoming}}|(){
    match stream {
        Ok(stream) => {
            let handle = |\textbf{\textcolor{kwgreen}{thread}}|::|\textbf{\textcolor{funblue}{spawn}}|((move || {
                let context = SetupContext {
                        ...
                };
                _ = |\textbf{\textcolor{funblue}{run\_state\_machine}}|(context);
            });
            handle.|\textbf{\textcolor{funblue}{join}}|();
        }
    }
}
\end{minted}

\caption{Parts of the setup state of the server}
\label{lst:setup}
\end{listing}

\begin{listing}[H]
    \centering
\begin{minted}[fontsize=\small, escapeinside=||, linenos]{c}
|\textbf{\textcolor{funblue}{SEC}}|("tracepoint/net/net_dev_xmit")
int |\textbf{\textcolor{funblue}{handle\_net\_dev\_xmit}}|(struct trace_event_raw_net_dev_xmit *ctx) {
    int pid = |\textbf{\textcolor{funblue}{bpf\_get\_current\_pid\_tgid}}|() >> 32;
    char comm[16] = {};
    |\textbf{\textcolor{funblue}{bpf\_get\_current\_comm}}|(&comm, sizeof(comm));
    
    if (|\textbf{\textcolor{funblue}{\_\_builtin\_strcmp}}|(comm, "server") == 0) {
        struct sk_buff *skb = (struct sk_buff *)ctx->skbaddr;
        char *head;
        |\textbf{\textcolor{typblue}{u16}}| mac_header;

        |\textbf{\textcolor{funblue}{member\_read}}|(&head, skb, head);
        |\textbf{\textcolor{funblue}{member\_read}}|(&mac_header, skb, mac_header);

        char *ip_header_address = head + mac_header + MAC_HEADER_SIZE;
        struct iphdr iph;
        |\textbf{\textcolor{funblue}{bpf\_probe\_read}}|(&iph, sizeof(iph), ip_header_address);

        if (iph.protocol != IPPROTO_TCP)
            return 0;

        |\textbf{\textcolor{typblue}{u32}}| src_ip = |\textbf{\textcolor{funblue}{\_\_builtin\_bswap32}}|(iph.saddr);
        |\textbf{\textcolor{typblue}{u8}}| src_lat_ip = src_ip & 0xff;

        |\textbf{\textcolor{typblue}{u32}}| dst_ip = |\textbf{\textcolor{funblue}{\_\_builtin\_bswap32}}|(iph.daddr);
        |\textbf{\textcolor{typblue}{u8}}| dst_last_ip = dst_ip & 0xff;

        if (src_lat_ip == 1 && dst_last_ip == 43) {
            |\textbf{\textcolor{typblue}{u8}}| ip_header_len = iph.ihl * 4;

            char *tcp_header = ip_header_address + ip_header_len;

            struct tcphdr tcph = {};
            |\textbf{\textcolor{funblue}{bpf\_probe\_read}}|(&tcph, sizeof(tcph), tcp_header);

            |\textbf{\textcolor{typblue}{u8}}| tcp_header_len = tcph.doff * 4;

            char *payload = tcp_header + tcp_header_len;

            struct Message msg = {};
            |\textbf{\textcolor{funblue}{bpf\_probe\_read}}|(&msg, sizeof(msg), payload);

            if (msg.msg_type < 0 || msg.msg_type > 5)
                return 0;

            ...
            |\textbf{\textcolor{funblue}{bpf\_ringbuf\_submit}}|(event, 0);
        }
    }

    return 0;
}
\end{minted}
\caption{Handling tracepoint in BPF}
\label{lst:bpf_tracepoint}
\end{listing}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/jitter_comparison_4_3__tcp.pdf}
    \caption{Presentation of the impact of the environment with average disturbance on the jitter of the TCP test cases.}
    \label{fig:mid_tcp}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/jitter_comparison_4_3__udp.pdf}
    \caption{Visualization of the impact of the environment with average disturbance on the jitter of the UDP test cases.}
    \label{fig:mid_udp}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/jitter_average_comparison_tcp.pdf}
    \caption{Illustration of the impact of different QoS settings on the average jitter of the TCP test cases.}
    \label{fig:qos_tcp_avg}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/jitter_average_comparison_udp.pdf}
    \caption{Presentation of the impact of different QoS settings on the average jitter of the UDP test cases.}
    \label{fig:qos_udp_avg}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/jitter_absolute_comparison_tcp.pdf}
    \caption{Visualization of the impact of different QoS settings on the maximum jitter of the TCP test cases.}
    \label{fig:qos_tcp_max}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/jitter_absolute_comparison_udp.pdf}
    \caption{Illustration of the impact of different QoS settings on the maximum jitter of the UDP test cases.}
    \label{fig:qos_udp_max}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/jitter_comparison_load_tcp.pdf}
    \caption{Presentation of the impact of the environment with high disturbance on the jitter of the TCP test cases.}
    \label{fig:high_tcp}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/jitter_comparison_load_udp.pdf}
    \caption{Illustration of the impact of the environment with high disturbance on the jitter of the UDP test cases.}
    \label{fig:high_udp}
\end{figure}

\begin{figure}[h]
  \centering
  \begin{subfigure}{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/radar_labor_max.pdf}
    \caption{Visualization of the weighting of influencing factors on the maximum jitter in the LDT.}
    \label{fig:r_labor_max}
  \end{subfigure}
\hfill
  \begin{subfigure}{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/radar_labor_med.pdf}
    \caption{Presentation of the weighting of influencing factors on the average jitter in the LDT.}
    \label{fig:r_labor_med}
  \end{subfigure}
  \caption{Illustration of the weighting of influencing factors on the maximum and average jitter in the LDT.}
  \label{fig:r_labor}
\end{figure}

\begin{figure}[h]
  \centering
  \begin{subfigure}{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/radar_high_max.pdf}
    \caption{Presentation of the weighting of influencing factors on the maximum jitter in the HDT.}
    \label{fig:r_high_max}
  \end{subfigure}
\hfill
  \begin{subfigure}{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/radar_high_med.pdf}
    \caption{Illustration of the weighting of influencing factors on the average jitter in the HDT.}
    \label{fig:r_high_med}
  \end{subfigure}
  \caption{Visualization of the weighting of influencing factors on the maximum and average jitter in the HDT.}
  \label{fig:r_high}
\end{figure}

\newpage


\begin{listing}[htbp]
    \centering
\begin{minted}[fontsize=\footnotesize, breaklines, linenos]{bash}
# Mark RT (1) and Bulk Traffic (3)
iptables -t mangle -A PREROUTING -i $IFACE -p tcp --dport $RT_PORT  -j MARK --set-mark 1
iptables -t mangle -A PREROUTING -i $IFACE -p tcp --sport $RT_PORT  -j MARK --set-mark 1
iptables -t mangle -A PREROUTING -i $IFACE -p udp --dport $RT_PORT  -j MARK --set-mark 1
iptables -t mangle -A PREROUTING -i $IFACE -p udp --sport $RT_PORT  -j MARK --set-mark 1

iptables -t mangle -A OUTPUT -o $IFACE -p tcp --dport $RT_PORT  -j MARK --set-mark 1
iptables -t mangle -A OUTPUT -o $IFACE -p tcp --sport $RT_PORT  -j MARK --set-mark 1
iptables -t mangle -A OUTPUT -o $IFACE -p udp --dport $RT_PORT  -j MARK --set-mark 1
iptables -t mangle -A OUTPUT -o $IFACE -p udp --sport $RT_PORT  -j MARK --set-mark 1

iptables -t mangle -A PREROUTING -i $IFACE -m mark --mark 0 -j MARK --set-mark 3
iptables -t mangle -A OUTPUT     -o $IFACE -m mark --mark 0 -j MARK --set-mark 3

#Set DSCP value
iptables -t mangle -A POSTROUTING -o $IFACE -p tcp --dport 8080 -j TOS --set-tos 0xB8
iptables -t mangle -A POSTROUTING -o $IFACE -p tcp --sport 8080 -j TOS --set-tos 0xB8
iptables -t mangle -A POSTROUTING -o $IFACE -p udp --dport 8080 -j TOS --set-tos 0xB8
iptables -t mangle -A POSTROUTING -o $IFACE -p udp --sport 8080 -j TOS --set-tos 0xB8

#QDisc: HTB
tc qdisc add dev $IFACE root handle 1: htb default 30

tc class add dev $IFACE parent 1: classid 1:1 htb rate 500mbit
tc class add dev $IFACE parent 1:1 classid 1:10 htb rate 470mbit ceil 500mbit prio 0
tc class add dev $IFACE parent 1:1 classid 1:30 htb rate 30mbit ceil 30mbit prio 5

tc filter add dev $IFACE parent 1: protocol ip handle 1 fw flowid 1:10
tc filter add dev $IFACE parent 1: protocol ip handle 3 fw flowid 1:30

#QDisc: Prio
tc qdisc add dev $IFACE root handle 1: prio bands 3

tc qdisc add dev $IFACE parent 1:1 handle 10
tc qdisc add dev $IFACE parent 1:2 handle 20
tc qdisc add dev $IFACE parent 1:3 handle 30

tc filter add dev $IFACE protocol ip parent 1: prio 1 u32 \
    match ip dport 8080 0xffff flowid 1:1

tc filter add dev $IFACE protocol ip parent 1: prio 1 u32 \
    match ip sport 8080 0xffff flowid 1:1

tc filter add dev $IFACE protocol ip parent 1: prio 5 u32 \
    match ip protocol 0 0 flowid 1:3
    
\end{minted}
\caption{QoS settings used in this work}
\label{lst:qos}
\end{listing}

%%% Local Variables: 
%%% mode: latex
%%% TeX-master: "thesis.tex"
%%% End: 
